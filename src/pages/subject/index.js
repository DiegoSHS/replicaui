import { Paper } from '@/components/mytail'

export const getServerSideProps = async ({ query: { id } }) => {
    
}
export default function Subject({ }) {
    return (
        <Paper>

        </Paper>
    )
}
